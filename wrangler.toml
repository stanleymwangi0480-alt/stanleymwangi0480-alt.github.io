name = "mystique-analytics"
main = "src/worker.js"
compatibility_date = "2026-01-01"

# Created by: wrangler kv namespace create ANALYTICS_KV
# Paste the "id" value that command prints into the line below.
[[kv_namespaces]]
binding = "ANALYTICS_KV"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"

# ADMIN_KEY is a secret, not a var — set it with:
#   wrangler secret put ADMIN_KEY
# Do not add it here in plaintext.
